import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import { spawnSync } from "node:child_process";
import test from "node:test";

const migration = await readFile(new URL("../../../supabase/migrations/20260920000000_fixed_pack_exact_size_receiving.sql", import.meta.url), "utf8");
const advisorCompatibility = await readFile(new URL("../../../supabase/migrations/20260921000000_historical_receipt_source_compatibility.sql", import.meta.url), "utf8");
const container = `vault-b22c-${process.pid}`;
const id = (n) => `00000000-0000-0000-0000-${String(n).padStart(12, "0")}`;
let started = false;
function docker(args, input) { const r = spawnSync("docker", args, { input, encoding: "utf8" }); if (r.status) throw Error(r.stderr || r.stdout); return r.stdout.trim(); }
function sql(q) { return docker(["exec", "-i", container, "psql", "-v", "ON_ERROR_STOP=1", "-U", "postgres", "-d", "b22c", "-t", "-A"], q); }
function fails(q) { const r = spawnSync("docker", ["exec", "-i", container, "psql", "-v", "ON_ERROR_STOP=1", "-U", "postgres", "-d", "b22c", "-t", "-A"], { input: q, encoding: "utf8" }); assert.notEqual(r.status, 0); return r.stderr; }
const receipt = (key, allocations) => `select status||'|'||fully_received from public.record_vault_purchase_order_receipt('${id(10)}','${id(1)}','2026-09-12','${id(2)}','${key}',$json$[{"purchase_order_line_id":"${id(20)}","allocations":${JSON.stringify(allocations)}}]$json$::jsonb);`;

test("B22C exact fixed-pack receipts preserve missing sizes as outstanding", async (t) => {
  t.after(() => { if (started) docker(["rm", "-f", container]); });
  docker(["run", "--rm", "-d", "--name", container, "-e", "POSTGRES_PASSWORD=x", "-e", "POSTGRES_DB=b22c", "postgres:17"]); started = true;
  for (let i = 0; i < 40; i++) { try { sql("select 1"); break; } catch { if (i === 39) throw Error("database not ready"); await new Promise((r) => setTimeout(r, 250)); } }
  sql(`create extension pgcrypto;create role anon;create role authenticated;create role service_role;create table public.vault_operators(id uuid primary key,is_active boolean not null);create table public.vault_locations(id uuid primary key,source text,active boolean,source_location_id text);create table public.vault_purchase_orders(id uuid primary key,status text not null,received_at timestamptz);create table public.vault_variants(id uuid primary key,product_id uuid not null,source text not null,source_active boolean not null,identity_resolution_status text not null,model_design text,normalized_size text,source_variant_id text,source_inventory_item_id text);create table public.vault_purchase_order_lines(id uuid primary key,purchase_order_id uuid not null references public.vault_purchase_orders(id),source_recommendation_type text,style_id text,recommended_units integer,recommended_packs integer,units_per_pack integer);create table public.vault_purchase_order_line_size_allocations(id uuid primary key,purchase_order_line_id uuid not null references public.vault_purchase_order_lines(id),parent_product_id uuid not null,model_design text not null,normalized_size text not null,variant_id uuid not null references public.vault_variants(id),shopify_variant_id_snapshot text not null,shopify_inventory_item_id_snapshot text not null,units_per_pack integer not null,ordered_units integer not null);create table public.vault_purchase_order_receipts(id uuid primary key default gen_random_uuid(),purchase_order_id uuid not null references public.vault_purchase_orders(id),received_location_id uuid,shopify_location_id_snapshot text,received_date date not null,created_by_operator_id uuid not null references public.vault_operators(id),idempotency_key text not null,created_at timestamptz not null default now(),unique(purchase_order_id,idempotency_key));create table public.vault_purchase_order_receipt_lines(id uuid primary key default gen_random_uuid(),receipt_id uuid not null references public.vault_purchase_order_receipts(id),purchase_order_line_id uuid not null references public.vault_purchase_order_lines(id),quantity_received integer not null,non_sellable_quantity integer not null default 0,discrepancy_note text);create table public.vault_purchase_order_receipt_allocations(id uuid primary key default gen_random_uuid(),receipt_line_id uuid not null references public.vault_purchase_order_receipt_lines(id),variant_id uuid not null references public.vault_variants(id),shopify_variant_id_snapshot text not null,shopify_inventory_item_id_snapshot text not null,quantity_received integer not null,non_sellable_quantity integer not null default 0,purchase_order_line_size_allocation_id uuid references public.vault_purchase_order_line_size_allocations(id));insert into public.vault_operators values('${id(1)}',true);insert into public.vault_locations values('${id(2)}','shopify',true,'loc');insert into public.vault_purchase_orders values('${id(10)}','ordered',null);insert into public.vault_variants values('${id(101)}','${id(50)}','shopify',true,'resolved','Blue','S','sv-s','ii-s'),('${id(102)}','${id(50)}','shopify',true,'resolved','Blue','M','sv-m','ii-m');insert into public.vault_purchase_order_lines values('${id(20)}','${id(10)}','fixed_pack_purchase_recommendation','${id(50)}::Blue',2,2,1);insert into public.vault_purchase_order_line_size_allocations values('${id(201)}','${id(20)}','${id(50)}','Blue','S','${id(101)}','sv-s','ii-s',1,1),('${id(202)}','${id(20)}','${id(50)}','Blue','M','${id(102)}','sv-m','ii-m',1,1);`);
  sql(migration);
  assert.equal(sql(receipt("partial", [{ purchase_order_line_size_allocation_id: id(202), quantity_received: 1 }])), "ordered|false");
  assert.equal(sql(`select count(*) from public.vault_purchase_order_receipt_allocations where purchase_order_line_size_allocation_id='${id(201)}'`), "0");
  assert.equal(sql(receipt("finish", [{ purchase_order_line_size_allocation_id: id(201), quantity_received: 1 }])), "received|true");
  assert.equal(sql(receipt("finish", [{ purchase_order_line_size_allocation_id: id(201), quantity_received: 1 }])), "received|true");
  assert.match(fails(receipt("over", [{ purchase_order_line_size_allocation_id: id(201), quantity_received: 1 }])), /cannot be received from status/);
  sql(advisorCompatibility);
  sql(`insert into public.vault_purchase_orders values('${id(11)}','ordered',null);insert into public.vault_purchase_order_lines values('${id(21)}','${id(11)}','advisor','${id(50)}::Blue',2,2,1);`);
  const advisorReceipt = (key, quantity, nonSellable = 0) => sql(`select status||'|'||fully_received from public.record_vault_purchase_order_receipt('${id(11)}','${id(1)}','2026-09-12','${id(2)}','${key}',$json$[{"purchase_order_line_id":"${id(21)}","non_sellable_quantity":${nonSellable},"allocations":${quantity ? `[{"variant_id":"${id(101)}","quantity_received":${quantity}}]` : "[]"}}]$json$::jsonb);`);
  assert.equal(advisorReceipt("advisor-partial", 1), "ordered|false");
  assert.equal(sql(`select purchase_order_line_size_allocation_id is null from public.vault_purchase_order_receipt_allocations a join public.vault_purchase_order_receipt_lines l on l.id=a.receipt_line_id where l.purchase_order_line_id='${id(21)}'`), "t");
  assert.equal(advisorReceipt("advisor-complete", 0, 1), "received|true");
  assert.match(fails(`select * from public.record_vault_purchase_order_receipt('${id(11)}','${id(1)}','2026-09-12','${id(2)}','bad',$json$[{"purchase_order_line_id":"${id(21)}","allocations":[{"variant_id":"${id(102)}","quantity_received":1}]}]$json$::jsonb);`), /cannot be received from status/);
});

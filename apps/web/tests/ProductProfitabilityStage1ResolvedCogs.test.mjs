import assert from 'node:assert/strict'; import {readFile} from 'node:fs/promises'; import test from 'node:test';
const sql=await readFile(new URL('../../../supabase/migrations/20261040000000_product_profitability_stage1_resolved_cogs.sql',import.meta.url),'utf8');
test('profitability consumes exactly one authoritative Stage 1 COGS resolution',()=>{assert.match(sql,/join public\.vault_stage1_sold_line_cogs_resolutions resolved on resolved\.order_line_id=l\.id/);assert.match(sql,/resolved_cogs_gbp,evidence_method,cogs_classification,provenance_id/);assert.doesNotMatch(sql,/cogs_status='trusted'/);assert.match(sql,/canonical_net_revenue=d\.merchandise_revenue_basis\+d\.customer_shipping_revenue/);});
test('coverage-line dependency is dropped and recreated without cascade',()=>{assert.match(sql,/drop view public\.vault_shopify_product_profitability_coverage_lines;[\s\S]*drop view public\.vault_shopify_verified_product_profitability_line_allocations;/);assert.match(sql,/create view public\.vault_shopify_product_profitability_coverage_lines/);assert.doesNotMatch(sql,/drop view[\s\S]*cascade/i);});
